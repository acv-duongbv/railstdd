<%= link_to "Create a product", new_product_path %>
<table>
    <th>title</th>
     <% @products.each do |product| %>
      <tr>
        <td><%= product.title %></td>
        <td><%= product.description %></td>
      </tr>
    <% end %>

</table>